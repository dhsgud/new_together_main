import 'package:flutter/material.dart';

class HotBoardPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('HOT 게시판'),
      ),
      body: Center(
        child: Text('HOT 게시판 페이지 내용'),
      ),
    );
  }
}
